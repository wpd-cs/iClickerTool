import os
import requests

if __name__ == "__main__":
    # Main function

    URL = 'https://student.iclicker.com/#/login'

    with open("../creds.txt") as f:
        lines = f.readlines()

        for line in lines:
            values = 
            os.system("start chrome --guest")
            print(line.replace('\n', '').split(','))

    # for x in range(3):
    #     os.system("start chrome --guest")
    